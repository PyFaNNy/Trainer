@{
    ViewData["Title"] = "Пациенты";
}
<h4>Пациенты</h4>
<form id='patients' method="post">
    @if (User.IsInRole("admin"))
    {
        <button type="submit" class="btn btn-primary m-1" asp-controller="Patient" asp-action="DeleteModel">Удалить</button>
        <a class="btn btn-primary m-1" asp-controller="Patient" asp-action="AddModel">Создать</a>
        <a class="btn btn-primary m-1" asp-controller="Patient" asp-action="ExportToCSV">Экспортировать в CSV</a>
    }

    <table class="table table-striped table-hover">
        <tr>
            <th><a asp-controller="Patient" asp-action="GetModels" asp-route-sortOrder="@ViewBag.LastNameSort">Фамилия</a></th>
            <th><a asp-controller="Patient" asp-action="GetModels" asp-route-sortOrder="@ViewBag.FirstNameSort">Имя</a></th>
            <th><a asp-controller="Patient" asp-action="GetModels" asp-route-sortOrder="@ViewBag.MiddleNameSort">Отчество</a></th>
            <th><a asp-controller="Patient" asp-action="GetModels" asp-route-sortOrder="@ViewBag.AgeSort">Возраст</a></th>
            <th><a asp-controller="Patient" asp-action="GetModels" asp-route-sortOrder="@ViewBag.SexSort">Пол</a></th>
            @if (User.IsInRole("doctor"))
            {
                <th></th>
            }
            @if (User.IsInRole("admin"))
            {
                <th></th>
                <th><input type="checkbox" name="cb_all"></th>
            }
        </tr>

        @if (Model != null)
            @foreach (var user in Model)
            {
                <tr onclick="window.location.href='/Patient/GetModel?id=@user.Id';">
                    <td>@user.LastName</td>
                    <td>@user.FirstName</td>
                    <td>@user.MiddleName</td>
                    <td>@user.Age</td>
                    <td>@user.Sex</td>
                    @if (User.IsInRole("admin"))
                    {
                        <td><a class="btn btn-primary" asp-controller="Patient" asp-action="UpdateModel" asp-route-id="@user.Id">Редактировать</a></td>
                        <td><input type="checkbox" class="patient" name="selectedPatients" value="@user.Id" /></td>
                    }
                    @if (User.IsInRole("doctor"))
                    {
                        <td><a class="btn btn-primary" asp-controller="Examination" asp-action="AddModel" asp-route-id="@user.Id">Назначить сеанс</a></td>
                    }
                </tr>
            }
    </table>
</form>

<script src="~/js/Views/Patient/GetModels.js"></script>